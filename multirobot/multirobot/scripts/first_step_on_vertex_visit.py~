#!/usr/bin/env python3
import numpy as np

for i in range(updates)
   I_intermediate = Merge_matrices(column_vector,IRk_previous)
   I = Merge_matrices(I_intermediate,Ivj_previous)
   Irk_current=Ivj_current=Order_matrix(I)
